name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install WiX
        run: |
          dotnet tool install --global wix --version 5.0.2
          wix extension add -g WixToolset.UI.wixext/5.0.2

      - name: Build AutoHDR
        run: dotnet build AutoHDR/AutoHDR.csproj -c Release

      - name: Build installer
        run: |
          cd AutoHDR.Installer
          wix build Package.wxs -arch x64 -o AutoHDR-1.1.1.msi -ext WixToolset.UI.wixext
          Move-Item AutoHDR-1.1.1.msi ..

      - name: Create portable zip
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path _release -Force
          $files = @(
            "AutoHDR/bin/Release/net48/AutoHDR.exe",
            "AutoHDR/bin/Release/net48/AutoHDR.exe.config",
            "AutoHDR/bin/Release/net48/known_games.txt",
            "AutoHDR/bin/Release/net48/store_game_publishers.txt"
          )
          Compress-Archive -Path $files -DestinationPath _release/AutoHDR-1.1.1-portable.zip -Force

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            AutoHDR-1.1.1.msi
            _release/AutoHDR-1.1.1-portable.zip
          body_path: Releases/v1.1.1/release-notes.md
